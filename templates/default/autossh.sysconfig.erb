# autossh instance definitions
#
# Autogenerated by Chef.

# Default autossh client username
#AUTOSSH_USER="autossh"
<% unless @autossh_user.nil? -%>
AUTOSSH_USER="<%= @autossh_user %>"
<% end -%>

# Default AUTOSSH_POLL
#AUTOSSH_POLL="5"
<% unless @autossh_poll.nil? -%>
AUTOSSH_POLL="<%= @autossh_poll %>"
<% end -%>

# Autossh specific command line options
#AUTOSSH_OPTS="-M 0"
<% unless @autossh_opts.nil? -%>
AUTOSSH_OPTS="<%= @autossh_opts %>"
<% end -%>

# Default SSH options
#SSH_OPTS="-vv -f -N"

# Typical defaults for add-on SSH options
#SSH_OPTS="$SSH_OPTS -p 22"
#SSH_OPTS="$SSH_OPTS -l ${AUTOSSH_USER}"
#SSH_OPTS="$SSH_OPTS -i /home/${AUTOSSH_UESR}/.ssh/id_dsa"
<% unless @ssh_opts.nil? -%>
SSH_OPTS="<%= @ssh_opts %>"
<% end -%>

# Forwarding to remote mysql port only from localhost, set as neeeded
#SSH_LPORTS="-Llocalhost:L2222:remote.domain:3306"
<% unless @ssh_lports.nil? -%>
SSH_LPORTS="<%= @ssh_lports %>"
<% end -%>

# Reverse forwarding to mysql port only from remote localhost, set as needed
#SSH_RPORTS="-Rlocalhost:4444:local.domain:3306"
<% unless @ssh_rports.nil? -%>
SSH_RPORTS="<%= @ssh_rports %>"
<% end -%>

# Target of SSH connection, may contain username or port as well
#SSH_HOST=localhost
#SSH_HOST=autossh@localhost.localdomain
#SSH_HOST=localhost.localdomain:22
<% unless @ssh_host.nil? -%>
SSH_HOST="<%= @ssh_host %>"
<% end -%>
